import React from 'react';
import {View, Text} from 'react-native';

const Cart = ({navigation}) => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Cart Page</Text>
    </View>
  );
};

export default Cart;
